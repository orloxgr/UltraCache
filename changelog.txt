## 2.57.95 - 2026-05-07
- Moves dependency-safety recommendations into the existing visible JS Delay / Defer Exclusions Populate Defaults flow instead of applying hidden runtime exclusion lists.
- Expands Populate Defaults with the previously internal JS dependency floor plus PrinceFrog/Davici 360-view and WooCommerce Google Analytics/GTag/GTM order-sensitive tokens.
- Defer/delay protection now honors the visible JS Delay / Defer Exclusions list; empty/custom-edited lists are no longer silently supplemented by built-in delay/defer exclusion fragments.

## 2.57.94 - 2026-05-07
- Strengthen JS Delay / Defer Exclusions enforcement for dependency-sensitive anonymous frontend errors.
- Add related defining-script protection for function/global exclusion tokens such as TreeSixtyImageRotate, including normalized camelCase/dashed matching and TreeSixty/ThreeSixty variants.
- Keep the jQuery / jQuery Migrate / legacy theme cluster ordered when user exclusions indicate legacy theme/plugin dependencies.
- No profile, CSS, font, LCP, object cache, or media optimization behavior changes.

## 2.57.93 - 2026-05-07
- Strengthen JS Delay / Defer Exclusions enforcement so matched exclusions protect the whole related script dependency group instead of only one exact tag.
- Protect related WordPress inline before/extra/after/translation scripts and nearby external dependencies when an exclusion matches an inline script, external script, global token, path fragment, or script id.
- Improve exclusion matching for camelCase versus dashed script/plugin fragments, helping tokens such as TreeSixtyImageRotate match equivalent URL fragments.
- No new switch, no new exclusion box, and no frontend behavior change unless JS delay/defer exclusions are configured.

## 2.57.92 - 2026-05-07
- Tighten Console Error Handler suggestions by filtering Chrome stack noise such as indexOf/index, jquery.min.js/jquery-migrate.min.js, malformed parenthesized filenames, and generic main/functions basenames while preserving targeted path fragments like themes/davici/js/functions.js.
- Make the Console Error Handler use the Runtime JS Scan suggestion engine through a dedicated admin REST parser, instead of a separate lightweight client-side parser.
- Avoid broad automatic exclusions from pasted console output, including same-origin domains, `jquery.min.js`, `main.js`, `functions.js`, generic `functions`, and broad `woocommerce` fragments.
- Improve pasted-console suggestions for missing globals, jQuery plugin method errors, WooCommerce Coupon Box params, Complianz globals, WooCommerce Google Analytics inline-after errors, and targeted theme/plugin path fragments.

## 2.57.90 - 2026-05-07
- Adds a Console Error Handler helper under JS Delay / Defer Exclusions. Paste browser console errors, extract clean script handles/files/plugin fragments/missing globals, and append missing suggestions to the existing JS Delay / Defer Exclusions list.
- Keeps one shared JS Delay / Defer Exclusions field; no new exclusion box is added.
- Expands recommended JS Delay / Defer defaults with additional WordPress inline translation/config handles and common dependency-sensitive plugin fragments such as Complianz, WooCommerce Coupon Box, WooCommerce Google, Contact Form 7, and media editor/view assets.

## 2.57.89 - 2026-05-07
- Protect WordPress inline script dependency groups during Defer all JS so localized `*-js-extra` / `*-js-before` / `*-js-after` snippets are not separated from their external script handle in anonymous optimized HTML.
- Strip native async/defer from dependency-sensitive script groups instead of forcing a defer that can make plugin globals such as localized params unavailable.

## 2.57.88 - 2026-05-07
- Replace the reported `parse_url()` call in the LCP image identity helper with WordPress-native `wp_parse_url()` to satisfy WordPress Coding Standards before public release.

## 2.57.87 - 2026-05-06
- Adds a visible **Delay all third-party JS** switch that delays external third-party scripts until interaction/fallback without adding another exclusion box. Existing Third-Party Delay Exclusions and JS Delay / Defer Exclusions both apply.
- Clarifies **Delay functional third-party JS** as **Delay known functional third-party JS** because it is pattern-based, while the new all-third-party mode is origin-based.
- Ensures third-party delay decisions run before plain defer in the final HTML path and mark delayed scripts with `data-ucwp-delay-reason="all-third-party"` when matched only by origin.
- Opens **Top CSS bundle sources by bytes** automatically after the user runs CSS diagnostics.

## 2.57.86 - 2026-05-06
- Improve manual LCP override selector support for simple CSS selectors such as `#hero > div.mask > img`, direct image selectors, and container selectors.
- Match Single LCP Image URL values against equivalent original/optimized image variants so manual AVIF/WebP URLs can still boost the matching rendered `<img>` tag.
- Force selected manual LCP images from lazy loading to eager/high priority and add explicit manual LCP markers such as `manual-selector`, `manual-selector-container`, and `manual-url-equivalent`.

## 2.57.84 - 2026-05-06
- Strengthen LCP Image Priority fallback detection for featured images in homepage/archive/singular layouts.
- Prioritize first .wp-post-image, post-thumbnail, featured-image, and wp-singular attachment/size image candidates without replacing manual/SR7/hero detection.
- Add LCP reason/score markers for non-SR7 featured-image candidates to make priority selection easier to verify.

## 2.57.82 - 2026-05-06
- Remove Critical Request Chain Relief and LCP Boundary Defer from the Balanced performance profile while keeping both features available as manual options.
- Improve LCP Image Priority by adding a guarded first/main `.wp-post-image` fallback for homepage, archive, and blog layouts without replacing existing manual hero, SR7, or slider detection.

## 2.57.81 - 2026-05-06
- Extend Local Google Fonts Optimization to rewrite Google Fonts @import rules found in loaded same-origin CSS, using the existing Google Fonts local cache and preserving the full stylesheet in optimized-css when needed.
- Include loaded same-origin CSS files in manual Google Fonts cache rebuild scans so CSS-level @import URLs can be discovered without scanning entire plugin/theme directories.
- Localize Google Fonts @import rules hoisted by CSS bundle generation and add Font Pipeline diagnostics for local/remote Google @import rewrites.

## 2.57.80 - 2026-05-06
- Synchronize successful Generate on Demand AVIF/WebP conversions into the persistent media conversion queue.
- Map on-demand source files back to attachment IDs and update best/avif/webp/both queue rows as skipped when fully optimized or pending when partially optimized.
- Keep on-demand queue synchronization best-effort and scoped to successful generation so frontend/warm-up rewrites are not blocked by queue mapping failures.

## 2.57.79 - 2026-05-06
- Apply performance profiles in two passes: profile settings are saved first, then Object Cache is set up separately.
- Object Cache backend probe failures no longer block the rest of the selected profile from applying.
- Profile object-cache probes return clean unavailable results instead of browser-level HTTP 500 responses for expected backend failures.
- Add clearer REST failure diagnostics with action/method/route/status details in dashboard errors.

## 2.57.78 - 2026-05-06
- Load real read-only Advanced Diagnostics during the dashboard bootstrap even when Cache Statistics are OFF.
- Keep stats polling and counter collection hard-disabled when Cache Statistics are OFF.
- Revert dashboard settings saves to the existing POST /settings REST route and remove the short-lived /settings/save route introduced in 2.57.77.

## 2.57.76 - 2026-05-06
- Keep Advanced Diagnostics visible when Cache Statistics are OFF.
- Show counter-only Advanced Diagnostics rows as Stats disabled while preserving diagnostics, path checks, storage checks, OPcache/APCu visibility, and the hard stop on stats polling/counter collection.

## 2.57.75 - 2026-05-06
- Align fresh-install defaults with the All Off profile by setting Lazy MailerLite nonce refresh and Delay icon font auto-detect OFF by default.
- Keep these optimizations available through Safe/Balanced/Aggressive profiles instead of enabling them silently on first install.

## 2.57.74 - 2026-05-06
- Fixed the Object Cache backend selector to use a dedicated three-column layout.
- Kept each Redis/APCu/Disk description inside its own column under the button.
- Preserved the race-safe exclusive selector behavior.

## 2.57.72 - 2026-05-06
- Polished Object Cache backend selector into compact Redis/APCu/Disk buttons.
- Selected backend now uses the existing green primary action style; unselected backends use the standard dark action style.
- Moved backend explanations outside the selector buttons and removed the selected-state badge text.

## 2.57.71 - 2026-05-06
- Replaced Object Cache backend switches with an exclusive Redis/APCu/Disk selector so rapid clicks cannot leave the backend on a stale toggle state.
- Passes dashboard work-in-progress state into the Object Cache card so backend/settings actions are blocked while saves are queued.
- Rewrote Quick start & examples as a concise best-results guide with Balanced-profile onboarding, CSS/JS testing guidance, scheduled warm-up cap notes, and multiline media WP-CLI commands.

## 2.57.70 - 2026-05-06
- Keeps OPcache and APCu runtime status visible when Cache Statistics are disabled.
- Keeps OPcache/APCu manual flush controls usable while cache counters, scans, polling, and dashboard stat collection remain hard-disabled.

## 2.57.69 - 2026-05-06
- Fixed Scheduled warm source counts by generating the saved crawl-scope summary from the freshly saved Warm Cache settings instead of a stale/default settings read.
- Fixed warm-scope normalization so already-normalized arrays (`sources`, `menuLocation`, `menuDepth`) are preserved when passed between summary helpers. This prevents selected Full-site warm-up sources from being dropped and reported as 0 discovered URLs.
- Avoids overwriting a previously stored crawl-scope summary with an invalid empty fallback if selected sources exist but summary generation is unavailable.
- Keeps Scheduled warm limit user-controlled; the source summary is informational and used to help choose the cap.

## 2.57.66 - 2026-05-06

- Fixed a dashboard crash where the Scheduled warm limit helper read `advancedForm` outside the React component scope.
- Clarified Cron Warm Up UI copy: cron warms HTML first, and when CSS Bundling is enabled missing bundles may be prepared before HTML is cached.

## 2.57.64 - 2026-05-06

- Restored Scheduled warm limit as an editable user-controlled cap instead of deriving/overwriting it from selected warm-up sources.
- Kept Full-site warm-up sources as informational source-count guidance for the scheduled warm limit, including the fixed priority order.
- Clarified that menu counts depend on the selected Menu warm-up menu and Menu depth.
- Renamed the UI label from “Menu warm-up menu” to “Menu warm-up”.

## 2.57.63 - 2026-05-06
- Corrected the Scheduled warm limit helper text so detected menu/content/base URL totals add up correctly and the global crawl cap is labelled clearly instead of being shown as the scheduled run total.

= 2.57.63 =
* Preserved user-maintained visible lists/textareas when applying performance profiles, including CSS bundle exclusions, JS delay/defer exclusions, CSS async exclusions, critical preload/delay lists, asset cleanup exclusions, cache exception lists, query allowlists, Google Fonts scan URLs, and manual LCP selectors.
* Preserved Redis connection infrastructure fields when applying profiles so Redis > APCu > Disk detection uses the configured Redis host/port/database/prefix/TLS/persistence/timeouts instead of resetting to profile defaults.
* Stopped performance profiles from appending detected query-string allowlist entries automatically; query allowlist population remains available from its dedicated UI action.
* Added a 10px visual gap between the Runtime Scan context label and dropdown while keeping the dropdown on the normal dashboard select styling.

= 2.57.60 =
* Runtime Scan now reports blocked/failed resources separately from JavaScript dependency errors.
* Resource load failures such as extension-blocked tracking/ads scripts are shown as “Blocked / failed resources” and are not counted as missing JS Delay / Defer exclusions.
* The displayed scan URL now removes ucwp_runtime_js_scan_context along with the other internal scan query args.
* Polished the Runtime Scan context dropdown to use the normal dashboard select styling and spacing.
* No changes to Safe Third-Party Delay patterns, LCP Boundary Defer behavior, FIFO queueing, cache, object-cache, Varnish, or media logic.


= 2.57.55 =
* AVIF/WebP Batch card now uses single-column action buttons, matching the Warm Cache layout.
* Media batch action explanations are plain helper text instead of separate boxed panels.
* Media conversion progress now appears in the shared job-progress position below the Warm Cache / AVIF-WebP row, matching warm-cache job placement.
* UI-only change; no cache, media conversion, object-cache, Varnish, REST, FIFO, CSS, or JS optimization logic changes.

= 2.57.54 =
* Dashboard layout polish: moved Profiles directly below the Stats panel in a single-column card.
* Profile card now shows Custom as a full-width row and the four presets in a two-column grid.
* Reordered the main dashboard flow to Stats → Profiles → Warm Cache / AVIF-WebP Batch → Cache Engine / Media Optimization, leaving the remaining sections unchanged.
* UI-only change; no cache, object-cache, Varnish, media, CSS, JS, REST, or FIFO action logic changes.

= 2.57.53 =
* Performance profiles now run object-cache backend detection sequentially in Redis > APCu > Disk order.
* Profile backend probes skip the runtime payload probe and test backend availability only.
* Indeterminate profile probe results stop the profile operation instead of silently choosing Disk.
* All Off skips object-cache and query allowlist detection.

= 2.57.52 =
* Fixed REST settings schema omission for lazyMailerliteNonceEnabled so the All Off profile and manual toggle can persist the Lazy MailerLite nonce refresh switch.
* No runtime cache, Varnish, object-cache, CSS, JS optimization, or media logic changes.

= 2.57.51 =
* Dashboard action lifecycle cleanup: switches, buttons, manual tests, and queued actions now enter a single FIFO UI operation pipeline.
* Persistent per-operation toasts remain visible while each queued operation is pending/running.
* Dashboard stats/diagnostics payloads are deferred until the operation queue is empty to avoid refresh races.
* Object Cache diagnostics: manual backend tests now include a safe-size object payload probe that stays below the configured payload limit.
* Object Cache diagnostics: payload probe reports the tested payload size and configured limit, instead of tripping the guard it is meant to validate.
* Object Cache UI: manual Redis/backend and object payload probe results persist after refresh and are not overwritten by passive diagnostics.
* Object Cache UI: fallback truth is clearer when APCu fallback is configured but unavailable; Redis active remains distinct from APCu availability.
* Dashboard UI: remove cyan helper backgrounds/borders from object-cache informational text and keep status values plain.

= 2.57.49 =
* Object Cache UI: removed passive “Not tested yet” Redis connection/Object payload probe rows from the dashboard status block.
* Object Cache UI: hides passive Redis payload guard rows from the status block; oversized payload skips remain internal guard behavior, not a Redis connection warning.
* Object Cache UI: status text is rendered without background/pill styling; the cyan 5% background helper is now transparent.

= 2.57.47 =
* Final PHPCS polish: replace the remaining production-scan `parse_url()` fallback with `wp_parse_url()`.
* Render managed runtime config without `var_export()` so production scans no longer flag debug/development output helpers.

= 2.57.46 =
* Final public-readiness packaging polish: updated WordPress readme changelog/upgrade notice and package metadata.
* Replaced private-build source wording with neutral public wording.
* Added index guards to top-level plugin includes/templates directories.

= 2.57.45 =
* Varnish UI polish: move the admin-mode security and current-mode helper copy to the bottom of the Varnish card.
* Restyle the admin-mode security helper copy as plain card description text without warning border/background.

= 2.57.44 =
* Removed the development-only Varnish debug log feature from settings, dashboard UI, schema, and runtime code.
* Redacted Varnish result detail strings before storing/displaying purge/test results.
* Escaped Varnish BAN URL expressions so URL purges match literal paths/queries instead of raw regex fragments.

= 2.57.43 =
* Object cache diagnostics: add passive/manual-test-only clarity flags to the default dashboard object-cache diagnostics payload, matching the stats-disabled payload.
* Keep fallback truth UI metadata consistent without reintroducing automatic live Redis/payload probes.

= 2.57.42 =
* Object cache backend truth / UI clarity hardening.
* Keeps passive selected/active/fallback backend status visible even when cache stats are disabled, without object-cache counts or live backend probes.
* Treats runtime-only object cache as an active fallback state, not as disk and not as a false success.
* Updates dashboard status tone so persistent fallbacks and runtime-only fallback show as warning while healthy selected=active remains success.

= 2.57.41 =
- Hardened CSS bundle stale-reference handling in the advanced-cache drop-in so cached HTML referencing missing generated CSS/font/optimized CSS assets is invalidated instead of served as HIT/STALE.
- Added CSS bundle file verification after atomic writes so manifest entries are only returned after the generated bundle is readable, non-empty, and matches the expected content hash.
- Added stricter delayed-icon-font companion validation so failed delayed-font writes do not produce stale companion metadata.

= 2.57.40 =
- Refined Runtime JS Scan reporting by redacting internal diagnostic URL parameters before storing/displaying report URLs.
- Kept review/manual Runtime JS Scan suggestions non-appendable while preserving them for manual review.
- Derived failing script sources from stack/detail text and surfaced raw captured browser errors in the dashboard.

= 2.57.39 =
- Hardens cache-poisoning bypass parity between the WordPress engine and advanced-cache drop-in.
- Adds non-removable security query guards for REST, nonce/token/order/payment/internal UltraCache control args.
- Adds advanced-cache cookie parity for wordpress_sec_ and hard protected frontend paths for REST/login/admin/WooCommerce endpoints.
- Treats invalid public ucwp_* internal control/profile/revalidate requests as bypass-only instead of canonical cacheable requests.

= 2.57.38 =
- Hardened object-cache drop-in recursive cleanup with explicit symlink-directory hard-stops.
- Hardened uninstall directory cleanup to avoid recursive WP_Filesystem directory deletes and skip symlinks.
- Narrowed runtime secret filesystem allowlist to the canonical off-docroot secret path and its atomic temp sibling only.

= 2.57.37 =
* Restricts automatic page-cache/object-cache/runtime-config full reconcile writes to WP-CLI or admins with both manage_options and activate_plugins.
* Hardens same-site frontend loopback URL validation so warm/profile/inspect flows do not accept arbitrary explicit ports on the site host.
* Redacts internal UltraCache runtime/profile query tokens such as ucwp_rt from debug contexts.

= 2.57.36 =
* Converted managed runtime config from runtime-config.json to guarded runtime-config.php while keeping a legacy JSON read fallback for safe transition.
* Ensured the internal revalidate secret is stored in the off-docroot runtime secrets file without overwriting Redis/Varnish secrets.
* Applied restrictive 0600 permissions after runtime secret writes and aligned delete-all-data cleanup with the current off-docroot secret path.

= 2.57.35 =
- Removed the remaining dashboard render call to the deleted JSBundleDiagnosticsPanel component so the Advanced Diagnostics card can render after JS bundling removal.
- Kept the 2.57.34 hard removal of experimental JS bundling intact without changing JS defer, delay, runtime scan, LCP boundary defer, or main-thread relief behavior.

= 2.57.34 =
- Removed the experimental safe deferred JS bundling feature from runtime HTML rewriting, dashboard settings, diagnostics, and saved settings mapping.
- Kept JS defer, delay, runtime scan, LCP boundary defer, and main-thread relief behavior unchanged.
- Stopped generating UltraCache js-bundles assets and removed js-bundles from generated-asset stale-reference validation/purge preservation paths.

= 2.57.33 =
- Hard-stops object-cache symlink traversal by using non-recursive empty-directory removal after symlink entries are skipped.
- Updates the core safe directory removal helper to avoid WP_Filesystem recursive delete behavior that can follow symlinked directories.
- Keeps managed media temp files from 2.57.32 under wp-content/cache/ultracache/tmp/media/.

= 2.57.32 =
- Moves GD AVIF/WebP probe temp files into a managed UltraCache temp directory under wp-content/cache/ultracache/tmp/media/.
- Cleans stale managed media temp files through the guarded filesystem helpers instead of relying on system temp cleanup.
- Adds initial object-cache cleanup/delete recursion symlink skip checks.

= 2.57.31 =
- Isolated early drop-in filesystem helpers so advanced-cache/object-cache templates no longer shadow the core WP_Filesystem/path-guarded ucwp_safe_* helpers.
* Allow explicitly configured external Redis and Varnish infrastructure hosts/ports instead of local/private-only blocking.
* Keep syntax validation and obvious public frontend endpoint blocking for Varnish HTTP targets.
* Keep stronger destructive file/plugin mutation capability gates from 2.57.29.

= 2.57.29 =
- Added Redis socket target validation for Redis tests and runtime Redis connections.
- Added stronger capability gates for destructive file/plugin mutations such as delete-all-data and conflicting drop-in removal.

= 2.57.28 =
- Hardens manual STORE/callback profiler activation so public query/header flags require the same signed runtime token used by authenticated dashboard profiler requests.
- Routes engine profiler activation through the shared token-gated helper to avoid a second unauthenticated activation path.

= 2.57.27 =
- Hardens public-release filesystem handling by adding a guarded chmod helper that prefers WP_Filesystem and updating Varnish/object-cache permission writes to use it.
- Allows safe custom local/private Varnish HTTP/admin ports while continuing to block public frontend endpoints such as :80/:443 and unsafe socket targets.
- Updates uninstall cleanup to prefer WP_Filesystem reads/deletes before guarded native fallbacks.

= 2.57.26 =
- Adds a safe runtime-artifact cleanup pass for old UltraCache lock/test leftovers in wp-content/cache/ultracache/locks/.
- Adds `wp ultracache cleanup_artifacts` with dry-run, max-age, and structured output support.
- Scheduled cleanup now reports runtime artifacts deleted while avoiding active flock-held locks.

= 2.57.21 =
- Adds Elementor module load-order patterns to the visible JS Delay / Defer Populate Defaults list, including frontend modules, webpack runtime, common.min.js, and elementor-admin-bar.min.js.
- Teaches JS Delay / Defer Scan and Runtime Scan to suggest targeted Elementor path fragments instead of only broad basenames when elementorModules is missing.
- Keeps the Defer all JS behavior user-visible: the fix adds actionable visible exclusions/suggestions rather than silently protecting Elementor with a hidden override.

= 2.57.20 =
- Improves Runtime Scan semantic dependency mapping for browser console/runtime failures involving React/wp-element, Elementor modules, and wp-api-fetch inline-after blocks.
- Preserves non-URL browser pseudo-sources such as wp-api-fetch-js-after:3225 so inline WordPress dependency-order errors can generate actionable JS Delay / Defer exclusion suggestions.
- Groups Runtime Scan suggestions for React/wp-element and wp-api-fetch separately in the dashboard for clearer review before appending missing exclusions.

= 2.57.19 =
- UI polish: dashboard StatCard numeric values now use a dedicated targeted class with lighter 24px typography so large counters look more elegant without affecting other white text or generic Tailwind-style utility classes.
- Keeps the 2.57.18 dashboard stats zero-display guard and authenticated REST refresh behavior unchanged.

= 2.57.18 =
- Fixes dashboard stat cards so a passive bootstrap placeholder is never rendered as real zero counters while Count Cache stats is ON.
- Forces the first authenticated dashboard stats refresh through the REST stats endpoint when statistics are enabled, while preserving the zero-impact disabled path when they are OFF.
- Shows an explicit refreshing state until live counters arrive from REST.

= 2.57.17 =
- Fixes a stats-ON dashboard regression from 2.57.16 where the authenticated REST stats endpoint returned only the passive placeholder payload, causing the dashboard to show zero/missing counters even though engine counters existed.
- Keeps Count cache stats OFF as a hard zero-impact path: disabled stats still return the tiny disabled payload and do not trigger engine/storage/object-cache scans.
- Preserves the 2.57.15 purge-all.lock cleanup and 2.57.14 locks-directory preservation fixes.

= 2.57.16 =
- Hard-gated dashboard/REST cache stats when Count cache stats is OFF so stats endpoints return a tiny disabled payload instead of running engine stats, filesystem scans, Redis/APCu counts, manifest scans, or analytics reads.
- Made dashboard bootstrap use the disabled/passive stats payload instead of calling get_engine_stats() on load.
- Added a client-side stats-OFF guard so refreshStats() does not call the stats endpoint while Count cache stats is disabled.
- Stopped the automatic browser/frontend compression probe from running on dashboard load; probes remain manual-only.
- Kept the 2.57.15 purge-all.lock cleanup fix intact.

= 2.57.15 =
- Clarified Flush All Cache runtime lock lifecycle by removing the stale purge-all.lock file after the active flock is released.
- Kept the locks/ directory preserved during purge cleanup so active runtime lock handles are not orphaned mid-purge.
- Fixed WP-CLI full purge reporting so a failed/blocked purge_all no longer prints a false success message.
- Added a clearer REST failure message when Flush All Cache cannot acquire the active purge lock.

= 2.57.14 =
- Fixed Flush All Cache runtime lock lifecycle by preserving the UltraCache `locks/` directory during purge-all cleanup.
- Prevents purge-all from deleting its own active `purge-all.lock` while the PHP worker is still running.
- Kept the 2.57.13 generated asset validation regex fix intact.

= 2.57.13 =
- Fixed generated asset validation regex patterns so final HTML/cache validation no longer triggers preg_match_all() "Unknown modifier" warnings for UltraCache-generated CSS/JS references.
- Kept the 2.57.12 warm progress layout unchanged.

= 2.57.12 =
- Moved the active warm-up progress panel below the Warm Cache and Media Optimization controls so running jobs no longer push the top action layout.
- Kept warm-up, media conversion, source selection, and cache logic unchanged.
= 2.57.11 =
- Reordered the Warm Cache card so homepage warm buttons appear first, followed by menu controls/buttons, then full-site source controls/buttons.
- Stacked Menu warm-up menu and Menu depth as separate full-width rows instead of placing them side-by-side.
- Changed Full-site warm-up sources from a multi-select list into a dropdown panel with switch toggles while preserving saved source choices.

= 2.57.10 =
- Shows all saved WordPress menus in the Menu Warm selector, with assigned/frontend menus listed first and unassigned saved menus listed separately.
- Keeps menu warm-up user-controlled: unassigned/demo menus are never scanned automatically and only run when selected.
- Preserves the 2.57.09 warm scope controls, menu depth selector, full-site source selector, and removal of CSS Bundle Summary from the Warm Cache card.

= 2.57.00 =
- Milestone RC polish release.
- Shows the Support this plugin card and support/contact buttons directly in the mobile dashboard flow instead of hiding them behind a mobile-only trigger.
- Keeps the 2.56.255 Plugin Check filesystem compliance fix intact.

= 2.56.255 =
- Fixed a Plugin Check filesystem compliance finding in the Disk Object Cache backend probe by using the WordPress-aware writable-path helper instead of a direct PHP writability check.

= 2.56.250 =
- Refactors dashboard work actions to run synchronously from the initiating REST request instead of the heavy worker/lock queue, preventing stale purge_all locks and premature completion states.
- Locks the admin dashboard while direct work actions run and applies profile switches as blocking actions that save immediately after object-cache detection and query-string whitelist append.

= 2.56.248 =
- Split dashboard heavy action execution from status polling: enqueue creates the job, a dedicated runner endpoint executes it, and the status endpoint is now read-only.
- Prevented Flush, Warm, Diagnostics, and other heavy actions from being marked complete/orphaned by the polling request while the worker is still running.

= 2.56.245 =
- Fix stale dashboard heavy-action lock recovery so stuck actions such as purge_all stop blocking the dashboard without SSH cleanup or install-time mutation.
- Reconcile heavy action locks against queued/running jobs during normal dashboard enqueue/status requests and release stale/mismatched locks safely.

= 2.56.244 =
- Fix Object Cache payload probe false warning immediately after backend/fallback switch by using a fresh selected/active backend probe when the current runtime object cache instance is stale.

= 2.56.243 =
- Fix Object Cache backend/fallback state isolation so selected backend, fallback backend, and Enable Object Cache do not overwrite each other during immediate saves.

= 2.56.242 =
- Fix Varnish/Redis runtime secret freshness after saving credentials so an immediate first test reads the newly written off-docroot secret file.
- Invalidate OPcache/stat cache after atomic runtime secret writes and before runtime secret reads, preventing stale secret reads in the next REST action.
- Make Varnish tests reset settings caches and prefer the fresh runtime secret source of truth without exposing secrets in REST, JS, HTML, or logs.

= 2.56.239 =
- Changed Query-string args whitelist Populate to build its list from registered/detected attributes, taxonomies, categories and tags in the current WordPress/WooCommerce setup instead of a generic hardcoded candidate list.
- Updated Query-string whitelist UI copy so Populate describes detected taxonomy/attribute query args.

= 2.56.238 =
* Inject the browser Runtime Scan collector after the unified final output pipeline instead of before JS optimization can alter it.
* Protect the Runtime Scan collector from inline JS delay handling, so scans that visibly count down do not return zero captured errors.
* Keep the existing wp_head collector as a compatibility fallback while final output injection removes/repositions duplicate collector markup.

= 2.56.235 - JS Runtime Scan Button UI Fix =
* Corrected the actual dashboard JS scan button labels to compact text: JS Delay / Defer Scan, Runtime Scan, Append Missing, Debug Profile, and Save.
* Applied the responsive JS scan action wrapper in the real component so buttons wrap instead of overlapping.
* Shows runtime scan status below the buttons.
* Keeps the 2.56.233 runtime scan cache-bypass behavior.

= 2.56.231 - Defer All JS Manual Empty Exclusions Workflow =
- Corrects Defer all JS so an empty JS Delay / Defer Exclusions list stays empty and all eligible scripts are deferred.
- Keeps WordPress/core dependency protections as visible Populate Defaults recommendations instead of automatic hidden or forced exclusions.
- Keeps inline after/translations dependency detection in JS Delay / Defer Scan so users can identify and append exclusions iteratively.

= 2.56.229 - Deterministic Diagnostics Output Buffer Fix =
- Forces the native WordPress 6.9 template enhancement output buffer only while UltraCache has selected an active cache/profile STORE path.
- Restores Speed Diagnostics, JS Delay / Defer Scan, and CSS Diagnostics timing-profile persistence through the unified final output pipeline.
- Adds profile checkpoints for template enhancement buffer forcing/start state to make future diagnostics failures easier to trace.

= 2.56.228 - Diagnostics Timing Profile Persistence Fix =
- Restores Speed Diagnostics / JS Delay-Defer Scan / CSS Diagnostics timing-profile persistence after the unified final output pipeline rewrite.
- Adds explicit per-run diagnostics profile IDs and writes both latest and run-specific profile JSON during STORE finalization.
- The dashboard profiler now waits for the exact diagnostic run profile instead of relying only on the latest profile file.

= 2.56.227 - Defer All JS Visible Exclusions Fix =
- Changes Defer all JS to behave literally: every eligible frontend script is deferred unless it matches the visible JS Delay / Defer Exclusions list.
- Removes hidden runtime slider/theme/safe-stage/core blocking from Defer all JS; WordPress/core dependency protections now belong in Populate Defaults as editable exclusions.
- Updates Populate Defaults for JS Delay / Defer Exclusions to add WordPress/core defaults, and to append slider defaults only when Fix sliders / hero sections is enabled.
- Keeps the 2.56.226 warm cache button UI polish, 2.56.225 generated asset root-relative URL fix, and 2.56.224 CSS bundle warm context fix intact.

= 2.56.226 - Warm Cache Button Width UI Polish =
- Updated the Warm Cache action layout so buttons remain vertical, compact, and left-aligned instead of being forced to full width.
- CSS-only admin UI polish; no cache, media, Redis/Varnish, OPcache, profile, public default, or performance pipeline changes.
- Kept the 2.56.225 generated asset root-relative URL fix, 2.56.224 CSS bundle warm context fix, and AVIF/WebP media rewrite context fix intact.

= 2.56.225 - RC Generated Asset Root-Relative URL Fix =
- Added final-output normalization for same-site UltraCache-generated asset URLs so cached HTML stores root-relative paths instead of absolute http/https URLs.
- Prevents mixed-content breakage in WP-CLI/loopback warm contexts where WordPress URL helpers may emit http:// URLs for HTTPS pages.
- Scope is limited to UltraCache-owned generated assets under cache/ultracache and uploads/uc-images; third-party, theme, plugin, and external service URLs are not changed.
- Kept the 2.56.224 CSS bundle warm context fix and the AVIF/WebP media rewrite context fix intact.
- No new performance features, profile changes, secret handling changes, or public defaults changes.

= 2.56.224 - RC CSS Bundle Warm Context Fix =
- Passed explicit target URL context through the final output pipeline during STORE and warm writes.
- Restored context-aware frontend performance optimization calls so CSS bundle replacement can resolve the correct manifest entry during WP-CLI/loopback warm storage.
- Kept the final media AVIF/WebP rewrite context fix intact.
- No new performance features, profile changes, secret handling changes, or public defaults changes.

= 2.56.223 - Plugin Check Final Cleanup =
- Added narrowly scoped PHPCS annotations for generated final-output stylesheet links and custom media queue table operations.
- No runtime, cache, media rewrite, Redis/Varnish, OPcache, profile, or performance pipeline changes.

= 2.56.218 - Final HTML Warm Storage + OPcache Refresh + Redis Autocomplete Fix =
- Shared the final frontend HTML rewrite pipeline between normal STORE requests and explicit warm writes, so cached HTML includes CSS bundle refs after CSS bundle warm actions.
- Kept force-refresh warm headers/cache-file cleanup from 2.56.217 and added finalizer processing before warm writes.
- Added visible OPcache Last flush status plus immediate and delayed OPcache stats refresh after Flush OPcache.
- Disabled browser autocomplete on Redis username/password fields.
- No performance feature was added.

= 2.56.209 - Disabled Cache Plugin Warning Fix =
- Split cache conflict UI so the removable warning box appears only for real non-UltraCache advanced-cache.php/object-cache.php drop-ins.
- Stopped disabled/installed cache plugin folders such as W3 Total Cache from keeping a persistent warning visible after conflicting drop-ins are removed.
- Kept active cache/performance plugin conflicts as dismissible toast warnings only.
- Forced diagnostics refresh after removing conflicting cache helpers so the drop-in warning disappears immediately when resolved.
- No cache/media/performance pipeline changes.

= 2.56.208 - Cache Helper Conflict Cleanup UX =
- Added detection for non-UltraCache advanced-cache.php/object-cache.php drop-ins with known/unknown owner reporting.
- Added backup-before-remove UI action for conflicting WordPress cache helper drop-ins only; plugin folders and settings are not deleted.
- Added Safe/Balanced/Aggressive profile support for the Lazy MailerLite nonce compatibility helper while keeping automations and inline CSS bundling off.
- No cache/media/performance pipeline changes.

= 2.56.207 - Public Metadata + Profile Preset Polish =
* Added public package requirement headers (`Requires at least` and `Requires PHP`) to the main plugin file.
* Shortened the plugin header and readme short descriptions for WordPress.org-style metadata.
* Updated performance profile descriptions to clarify public-safe behavior and visual-testing expectations.
* Kept automations/scheduled jobs/save-time warm actions off in every profile preset.
* Kept automatic media generation on upload/on demand off in profile presets; users can enable or run media rebuild manually.
* Kept Inline CSS Bundling off in every profile preset, including Aggressive.
* Kept the build scoped to metadata/profile preset polish only; no cache/media/performance pipeline behavior was changed.

= 2.56.206 - Public Package Cleanup =
* Synchronized root uninstall cleanup with the current UltraCache runtime data model, scheduled hooks, media queue table, queue options, and generated cache locations.
* Preserved the conservative uninstall policy that keeps optimized media variants under `wp-content/uploads/uc-images/` unless a site owner removes them manually.
* Verified the public WP-CLI command list/readme remains single-entry and package-ready.
* No new optimization feature was added.

= 2.56.205 - Action Queue Simplification RC Cleanup =
* Removed the unnecessary legacy action-queue scrub/migration behavior from job loading; private pre-release queue state can be cleared manually instead of migrated.
* Kept future action queue storage/output redaction for newly created jobs and results.
* Avoided activation/upgrade queue deletion logic and kept the build scoped to RC cleanup only.
* No new optimization feature was added.

= 2.56.204 - Public RC Secret Redaction Hotfix =
* Hardened dashboard action queue loading so existing stored jobs are normalized and scrubbed before being returned or saved again.
* Added stronger recursive redaction for nested diagnostics, runtime config snapshots, JSON-like strings, REST job payloads, and dashboard queue results.
* Removed raw runtime Redis/Varnish secret values from public diagnostics output while preserving configured/health status indicators.
* Kept the build scoped to RC hardening only; no new optimization feature was added.

= 2.56.203 - Public Release Candidate Hardening =
- Added public-release packaging files including GPL license and conservative uninstall cleanup behavior.
- Hardened dashboard action queue storage so sensitive credentials are redacted before jobs/results are persisted.
- Added stricter filesystem write/copy/rename/mkdir guards for generated assets, drop-ins, runtime files, wp-config helpers, and managed .htaccess updates.
- Hardened same-site loopback/profiler requests with safer URL validation and WordPress safe remote request defaults.
- Made core WooCommerce dynamic paths/query arguments a hard cache bypass rather than an optional site setting.
- Updated public readme/README guidance for experimental features, privacy/external-service behavior, and RC-safe defaults.

= 2.56.202 - JS Bundle Candidate Diagnostics =
- Added persistent diagnostics for the experimental Combine safe deferred JS feature.
- Records scanned/deferred/eligible script counts, rejection reasons, eligible groups, generated bundles, bundle bytes, and sample candidates.
- Exposes the last JS bundle result in dashboard diagnostics and `wp ultracache status --section=diagnostics`.
- Leaves frontend behavior unchanged except for reporting; conservative JS bundle safeguards remain in place.

= 2.56.201 - Experimental Safe Deferred JS Bundle =
* Added an opt-in Advanced Settings & Exclusions switch for combining only consecutive, same-host deferred JavaScript files into generated UltraCache JS bundles during cache warm/store.
* Added visible JS bundle include and exclude pattern lists; exclusions win over includes and the shared JS Delay / Defer Exclusions list remains authoritative.
* Added conservative safeguards for inline before/after scripts, WordPress core dependencies, WooCommerce critical scripts, Slider/Elementor protected scripts, modules, async scripts, missing source files, and non-local scripts.

= 2.56.200 - Real AVIF Capability Guard =
- Validates GD AVIF support with a real suppressed encode probe and rejects 0-byte/invalid AVIF outputs.
- Keeps AVIF generation on Imagick when Imagick AVIF is available instead of falling through to GD/WP_Image_Editor_GD.
- Suppresses GD imageavif warnings during support probes/conversion and skips GD AVIF saves when the real probe fails.
- Adds stricter generated AVIF/WebP file validation before optimized variants are accepted.
- Improves warm output when DONOTCACHEPAGE is set by debugging/logged-in tooling such as Query Monitor.

= 2.56.199 - Atomic Delayed Icon Font CSS =
* Fixed delayed icon-font extraction after mixed CSS moved to /optimized-css/: delayed-*.css is now always written to /font-css/ before the HTML link is injected.
* Added generated CSS link guards so missing delayed font, bundle delayed-font, font-css, and optimized-css references are not emitted after a clean CSS rebuild.
* If delayed icon font CSS cannot be written and verified, UltraCache keeps the original stylesheet instead of stripping @font-face and breaking icon glyphs.

= 2.56.198 - Mixed Active CSS Bucket for Delayed Icon Fonts =
* Fix: Delay icon font-face blocks no longer writes preserved mixed theme/plugin CSS into /cache/ultracache/font-css/.
* New: Preserved active CSS without delayed icon @font-face blocks is written under /cache/ultracache/optimized-css/ with active-* filenames.
* Guard: UltraCache generated optimized-css assets are excluded from CSS bundle/font rewrite source scans to prevent re-processing loops.

= 2.56.197 - Delayed Icon Font TTF to WOFF Rewrite =
* Expanded generic icon-font auto detection for local TTF font-face blocks such as feather/star icon fonts.
* Rewrites same-path local .ttf sources to .woff2 or .woff when available before delaying icon font-face CSS.
* Prevents local font-display patch CSS from duplicating icon-font TTF blocks when Delay icon font-face blocks is enabled.

= 2.56.195 - Generic Bundle Path TTF Cleanup =
- Fixed bundled/final HTML font optimization path so generic local @font-face .ttf declarations can be rewritten through the active bundle manifest WOFF2 registry.
- Added same-path .ttf to .woff2 preference where a valid local WOFF2 file exists.
- Kept the fix generic to local TTF references, not vendor-specific Themepunch targeting.

= 2.56.194 - Themepunch Slider Font TTF Cleanup =
- Rewrite Slider Revolution/Themepunch gfonts TTF font-face and config URL tokens to matching linked WOFF2 fonts when available.
- Keep RevSlider helper image preload suppression from 2.56.192/2.56.193.

= 2.56.193 - Restore Safe SR7 Module Background LCP Preload =
* Restores UltraCache-managed LCP preload for confirmed final sr7-module-bg background URLs present in cached HTML.
* Keeps the 2.56.192 guard that suppresses ambiguous /revslider/o/ helper-asset preloads to avoid Chrome unused-preload warnings.
* Keeps duplicate UltraCache image preload cleanup.
* Adds a small defensive fix for legacy optimized-image marker variables in LCP preload URL equivalence checks.

= 2.56.192 - RevSlider LCP Preload Guard =
- Suppresses UltraCache-managed LCP image preload tags for ambiguous SR7/RevSlider generated image-list helper assets under `/revslider/o/`.
- Prevents Chrome “preloaded but not used” warnings caused by generated RevSlider helper AVIF/WebP assets that are not consumed immediately by the SR7 runtime.
- Keeps image rewriting, on-demand generation, and runtime/fetchpriority LCP marking intact; only unsafe network preloads are suppressed.
- Deduplicates UltraCache-managed image preload links during LCP preload cleanup.

= 2.56.187 - Verified Optimized Image Output Hardening + WP-CLI Help =
* Hardened optimized image validation so zero-byte AVIF/WebP outputs are treated as invalid, ignored, and regenerated or fallen back safely.
* Existing optimized variants are now considered usable only when the file exists and has non-zero size.
* Added media diagnostics counters for zero-byte optimized files and invalid optimized output, while keeping AVIF generation Imagick-first and WebP as the safe fallback.
* Expanded the root `wp ultracache` command into a complete command reference grouped by cache, media, settings, diagnostics, cron, object-cache, and integrations.
* Added `wp ultracache help` and `wp ultracache commands --format=json` aliases for discoverability and scripting.
* Added `wp ultracache media help` and a user-friendly `wp ultracache media regenerate` action that rebuilds and processes the optimized media queue.

= 2.56.186 - On-Demand AVIF Failure WebP Fallback =
* Fixes safe warm/cron/stale/manual on-demand image resolution so a slow or failed AVIF generation attempt cannot prevent the same discovered upload image candidate from falling back to WebP.
* Keeps AVIF-first selection when AVIF exists or can be generated successfully.
* Preserves the shared optimized URL rewrite map for normal and escaped JSON/script URL forms.
* Does not add RevSlider-specific parsing or legacy cache image paths.

= 2.56.185 - Complete Escaped JSON Image Rewrite and AVIF Preference =
* Complete local upload image rewrite for escaped JSON/script URL tokens using the shared optimized URL map.
* Preserve escaped-slash JSON format while rewriting to root-relative persistent uploads/uc-images URLs.
* Prefer AVIF before WebP during warm/on-demand cache generation when the request bucket supports AVIF, falling back only if AVIF generation fails.

= 2.56.184 - Connect Existing Image Discovery to On-Demand Rewrite =
* Connects existing src/data-src/srcset/style/global upload URL discovery passes to the same on-demand optimized media resolver before cached HTML is saved.
* Adds escaped absolute upload URL token support for builder/slider JSON strings such as https:\/\/example.com\/wp-content\/uploads\/...png.
* Keeps persistent root-relative /wp-content/uploads/uc-images/ URLs and avoids legacy cache image paths.

= 2.56.184 - Generic CSS url() Upload Image Rewrite =
- Prioritized CSS url(...) background image rewrites before generic image sweeps so builder/slider inline background images are converted within the warm generation budget.
- Added support for HTML-entity quoted CSS URLs such as url(&quot;...png&quot;) without hardcoding Slider Revolution.
- Kept optimized image frontend URLs root-relative under /wp-content/uploads/uc-images/.

= 2.56.181 - Page-Specific Warm Image Generation Rewrite =
* Added a final root-relative uploads image URL sweep so warm/cache HTML can trigger missing AVIF/WebP generation for page-specific images, including URLs outside normal image attributes.
* Kept optimized media output under persistent /wp-content/uploads/uc-images/ paths with no legacy cache-directory fallback.
* Changed optimized media URL constants to root-relative paths for safer cached HTML behind HTTPS/reverse proxies.
- Outputs optimized AVIF/WebP URLs as root-relative /wp-content/uploads/uc-images/... paths to avoid HTTP/HTTPS scheme mismatch behind reverse proxies.
- Allows the final media rewrite pass to scan root-relative /wp-content/uploads/... image URLs during warm-up/render instead of requiring an absolute uploads base URL in the HTML.
- Keeps optimized AVIF/WebP media exclusively under uploads/uc-images with no legacy cache-dir fallback.

= 2.56.178 - Persistent Optimized Image Storage =
* Moved newly generated AVIF/WebP media variants from transient wp-content/cache folders to persistent wp-content/uploads/uc-images/avif and wp-content/uploads/uc-images/webp storage.
* Kept legacy cache-folder AVIF/WebP paths readable for migration/repair compatibility, but new frontend rewrites target uploads/uc-images.
* Added diagnostics metadata for persistent optimized media storage and legacy cache-media folders.
* No intended changes to object cache, JavaScript optimization, font optimization, or CSS bundle generation.

= 2.56.174 - CSS Bundle Cleanup Cached HTML Ref Protection =
* Protect orphan-like CSS bundle files during cleanup when existing cached HTML files still reference them.
* Added cached HTML CSS reference diagnostics for CSS bundle storage.
* No intentional change to bundle build, warm-up, media, or object-cache behavior.

= 2.56.171 - Media Converter Split =
* Refactored the large media converter class into dedicated media traits for diagnostics, background generation, persistent queue handling, conversion, path/URL resolution, and frontend HTML rewrites.
* Kept class-media-converter.php as a small orchestrator that loads and composes the new media traits.
* No intended behavior changes; this is a maintainability split preparing the media queue/REST/CLI regression pass.

= 2.56.170 - Object Cache Text Input Row Polish =
* Changed Object Cache Redis host, Redis password, and Redis prefix / namespace fields to use the same left-label/right-control row layout as Redis port and Redis database.
* Kept this as UI-only polish with no runtime/cache behavior changes.

= 2.56.169 - Numeric Row UI Cleanup and Automation/Varnish Layout Polish =
* Removed borders from numeric row controls so they visually match switch rows.
* Moved Object Cache fallback status below the fallback description and placed Redis host beside the fallback selector.
* Consolidated Automation & Scheduling controls into one grid and reordered them for a left-to-right workflow.
* Renamed cron warm-up auto-start switches for clearer intent.
* Moved Varnish Debug log beside Timeout (seconds) in the Varnish / Reverse Proxy grid.

= 2.56.168 - Automation Cleanup Settings and Numeric Input UI Polish =
* Added dashboard settings for CSS bundle cleanup grace window and CSS bundle cleanup delete limit under Automation & Scheduling.
* Cleanup grace now defaults to 48 hours with a safe 1–168 hour range; delete limit defaults to 60 files per cleanup run with a 5–500 range.
* CSS bundle cleanup still supports the existing advanced filters as an override layer.
* Updated storage diagnostics to show the cleanup policy as dashboard/filter based.
* Restyled single-line numeric input controls in Object Cache, Automation & Scheduling, and Varnish / Reverse Proxy to match switch-row layout with text on the left and compact inputs on the right.

= 2.56.167 - Regression Regex and Storage Diagnostics Polish =
- Fixed the full regression script guidance for AVIF/font-display HTML marker regex checks so false warnings are avoided.
- Improved CSS bundle storage diagnostics with human-readable cleanup grace/delete-limit labels, filter names, default values, bounds, and clearer grace-window protection messaging.
- Clarified when orphan-like CSS bundle files are protected by the cleanup grace window and are not yet eligible for scheduled cleanup.
- No cache, warm, CSS bundle, JS, font, LCP, media, Varnish, REST, WP-CLI, or storage cleanup behavior changes.

= 2.56.166 - Full Regression After Engine Split =
- Bumped the release-candidate package version after the completed engine trait split sequence.
- Added no new optimization behavior; this build is intended for full regression validation after splitting the engine into focused traits.
- Confirmed the package keeps the post-split engine structure, including storage, CSS bundle, font, LCP/slider, JS, media/image, HTML/output, cache decision, warm/crawl, profiling/metrics, analytics, drop-in lifecycle, and async CSS traits.
- Kept page cache, object cache, media queue, CSS bundle, font, LCP, JS defer/delay, Varnish, REST, and WP-CLI behavior unchanged.

= 2.56.165 - Final Engine Orchestrator Cleanup =
- Completed the final engine orchestrator cleanup by moving remaining analytics/statistics helpers into `includes/engine/class-engine-analytics-trait.php`.
- Moved advanced-cache drop-in, activation/deactivation, cache directory, and recursive cleanup helpers into `includes/engine/class-engine-dropin-lifecycle-trait.php`.
- Moved safe async CSS rewrite and stylesheet decision helpers into `includes/engine/class-engine-async-css-trait.php`.
- Reduced `includes/class-ultra-cache-engine.php` to a focused orchestrator/core runtime file of about 432 lines.
- Kept page-cache, warm/crawl, CSS bundle, JS defer/delay, font, LCP/slider, media/image, Varnish, REST, and WP-CLI behavior unchanged.

= 2.56.164 - Engine Profiling / Metrics Trait Split =
- Split store profiling, request checkpoint, performance profile, and async CSS profile diagnostics helpers into includes/engine/class-engine-profiling-metrics-trait.php.
- Reduced the main engine class by moving profiling/profile JSON read-write, stage timing, request checkpoint, critical request chain profile, and deferred post-response store profiling helpers out of includes/class-ultra-cache-engine.php.
- No intended behavior changes to page cache, warm/crawl, CSS bundle, JS, font, LCP, media, Varnish, REST, or WP-CLI behavior.

= 2.56.163 - Engine Warm / Crawl / Preload Trait Split =
- Moved cache warming, content-change invalidation, purge orchestration, loopback response validation, runtime lock helpers, related URL discovery, and crawl URL batching helpers out of the main engine class into includes/engine/class-engine-warm-crawl-trait.php.
- Kept warm, purge, crawl, inspect, cache decision, CSS bundle, font, JS, LCP/slider, media/image, REST, and WP-CLI behavior unchanged.
- Continued the release-candidate engine split into smaller feature traits without adding legacy compatibility layers.

= 2.56.162 - Engine Cache Decision / Bypass Trait Split =
* Split cache decision, bypass, query-string cache variant, local URL validation, request URL, inspection, internal revalidate, and preload-bypass helpers from `includes/class-ultra-cache-engine.php` into `includes/engine/class-engine-cache-decision-trait.php`.
* Kept existing cacheability, bypass, inspect, purge, warm, CSS, JS, font, LCP, media, REST, and WP-CLI behavior unchanged.

= 2.56.161 - Engine HTML Optimization / Output Trait Split =
- Moved generic frontend HTML rewrite orchestration, safe rewrite guards, HTML minification helpers, speculation-rules injection, protocol-relative URL normalization, tag-processor helpers, and authoring-asset cleanup helpers out of the main engine class into includes/engine/class-engine-html-output-trait.php.
- Kept cache output, CSS bundle, font, JS defer/delay, LCP/slider, media/image, purge, and warm behavior unchanged.
- Continued the release-candidate engine split into smaller feature traits without changing public REST or WP-CLI contracts.

= 2.56.160 - Engine Media / Image Optimization Trait Split =
- Moved safe CLS image dimension injection and local public image dimension resolution helpers out of the main engine class into includes/engine/class-engine-media-image-trait.php.
- Kept AVIF/WebP converted-image source mapping, attachment metadata lookup, file-dimension fallback, purge/warm behavior, and generated HTML output unchanged.
- Continued the release-candidate structural split of the engine into smaller feature traits without changing media queue or batch conversion behavior.

= 2.56.159 - Engine JS Optimization Trait Split =
- Moved JS defer/delay, defer-all final pass, third-party script delay, JS delay-safety diagnostics, delayed-script loader, and frontend asset-chain cleanup helpers out of the main engine class into includes/engine/class-engine-js-optimization-trait.php.
- Kept script defer/delay rules, exclusion handling, delayed-script markup, CSS bundle output, font output, purge/warm behavior, and frontend cache behavior unchanged.
- Continued the release-candidate structural split of the engine into smaller feature traits.

= 2.56.158 - Engine LCP / Slider Trait Split =
- Moved LCP Image Priority, LCP Boundary Defer, SR7/Slider Revolution first-slide detection, slider/hero protection helpers, manual critical preload helpers, and LCP preload/markup helpers out of the main engine class into includes/engine/class-engine-lcp-slider-trait.php.
- Kept LCP markers, fetchpriority/preload behavior, CSS bundle output, purge/warm behavior, and frontend cache behavior unchanged.
- Continued the release-candidate structural split of the engine into smaller feature traits.

= 2.56.159 - Engine Font Optimization Trait Split =
- Moved Google Fonts localization/swap helpers and self-hosted font CSS optimization helpers out of the main engine class into includes/engine/class-engine-font-optimization-trait.php.
- Kept generated font-css output, font-display patch behavior, Google Fonts cache behavior, and CSS bundle references unchanged.
- Continued the release-candidate structural split without changing frontend cache behavior.

= 2.56.156 - Engine CSS Bundle Trait Split =
- Moved the main CSS bundle build, manifest, cleanup, replacement, and leftover bundle helper methods out of the engine class into includes/engine/class-engine-css-bundle-trait.php.
- Kept CSS bundle behavior, manifest format, warm-up behavior, and generated HTML/CSS references unchanged.
- Continued the release-candidate structural split without changing frontend cache behavior.

## 2.56.155 - Engine Storage / Filesystem Trait Split

- Split selected page-cache storage and filesystem helper methods out of the large engine class into `includes/engine/class-engine-storage-trait.php`.
- Added `includes/engine/` with an index guard file.
- Kept cache path, variant write, early cached-file serving, stale CSS bundle reference validation, runtime lock-file path, and related storage helper behavior unchanged.
- No frontend cache, CSS, JS, font, Varnish, media, object cache, REST, or WP-CLI behavior changes intended.

## 2.56.154 - REST API Controller Split
- Split the REST API implementation into smaller controller/trait files under `includes/rest/`.
- Kept the same REST namespace and route contracts while reducing `includes/class-rest-api.php` to a focused bootstrap class.
- Added REST traits for schemas/routes, cache/settings/integration actions, media actions, action queue handling, performance profiler helpers, and shared REST helpers.
- No frontend cache, CSS, JS, font, media, Varnish, object cache, settings, or warm-up behavior changes intended.

# UltraCache Changelog

## 2.56.153 - WP-CLI Synopsis Fix
- Fixed WP-CLI synopsis annotations after the command split so `wp ultracache status --section=storage --format=json` is accepted again.
- Fixed the cleanup command synopsis so `wp ultracache cleanup` runs without requiring an action argument.
- No frontend, cache engine, CSS, JS, media, Varnish, or warm-up behavior changes.

## 2.56.152 - WP-CLI Command Split
- Split the WP-CLI command implementation into focused trait files under `includes/cli/`.
- Kept the existing `wp ultracache ...` command names, arguments, and behavior unchanged.
- Added CLI helper, cache, media, settings/stats, and integrations command groups for easier public-release maintenance.
- No frontend cache, warm-up, media, Varnish, object-cache, REST, dashboard, CSS, JS, or font behavior changes were intended.

## 2.56.151 - Diagnostics / Storage Trait Refactor
- Added `includes/diagnostics/class-diagnostics-trait.php`.
- Moved dashboard diagnostics, font diagnostics, storage diagnostics, CSS bundle summary diagnostics, runtime diagnostics, analytics diagnostics, and environment diagnostics helpers out of `ultracache.php`.
- Added `includes/diagnostics/index.php`.
- Kept public methods and diagnostics output contracts unchanged.
- No frontend cache, CSS/JS/font, Varnish, media, warm queue, or object-cache behavior changes intended.

## 2.56.150 - Varnish Integration Trait Refactor

- Moved Varnish and reverse-proxy integration methods out of the main plugin class file into `includes/integrations/class-varnish-trait.php`.
- Added `includes/integrations/index.php` for directory protection.
- Kept public APIs and behavior unchanged for Varnish purge, test, diagnostics, and reverse-proxy status handling.
- No changes to frontend cache output, page-cache storage, CSS/JS/font optimization, media queue, warm queue, object cache, or dashboard JavaScript behavior.

## 2.56.149
- Fixed Font Display Optimization diagnostics so generated `font-display-*.css` patch files are counted correctly.
- Added dashboard counters for font-display patch files, bytes, patched @font-face blocks, and swap declarations.
- No rewrite, cache, CSS bundle, JavaScript, media, Varnish, or object cache behavior changes.

## 2.56.148
- Renamed the dashboard “Google Fonts display=swap” switch to “Font Display Optimization” while keeping the existing `googleFontsSwapEnabled` setting key for backward compatibility.
- Generalized the switch so it can add `font-display: swap` to missing local `@font-face` declarations through a small generated font-display patch stylesheet.
- Kept the behavior generic for themes, plugins, WooCommerce/icon fonts, and local same-origin stylesheets instead of adding site-specific font fixes.
- Did not change JS defer/delay, CSS bundle scope, Varnish, media, warm queue, object cache, or page cache architecture.


## 2.56.147
- Font CSS extraction fix: generated `/wp-content/cache/ultracache/font-css/` files are now kept font-face-only.
- Mixed layout/theme stylesheets that contain `@font-face` are no longer copied wholesale into `font-css`; the original stylesheet is kept intact instead.
- Runtime font CSS map cache key bumped to avoid reusing old mappings that pointed to broad/mixed generated font-css files.
- Added font diagnostics for non-font generated CSS files/bytes and files without `@font-face`.
- Kept the 2.56.146 WOFF2 preference, duplicate `src` cleanup, duplicate `@font-face` cleanup, and conservative minification for pure font CSS.


## 2.56.146
- Added generated font CSS cleanup helpers under includes/fonts/ for conservative minification, duplicate @font-face/src cleanup, and WOFF2-preferred generated font CSS.
- Enhanced Font Pipeline diagnostics with generated font-css font-face, WOFF2, WOFF, TTF, and largest-file counters.
- Kept frontend cache, JS defer/delay, CSS bundle scope, Varnish, media queue, and warm queue behavior unchanged.

## 2.56.145

- Refactor: extracted dashboard settings/defaults/sanitization/client-settings methods from `ultracache.php` into `includes/settings/class-settings-trait.php`.
- Refactor: extracted admin dashboard/menu/assets/admin-bar/notice methods from `ultracache.php` into `includes/admin/class-admin-trait.php`.
- Added `includes/settings/index.php` and `includes/admin/index.php` for directory safety.
- Kept existing public method names through traits so REST, WP-CLI, dashboard, and engine callers continue using `Ultra_Cache_WP` unchanged.
- No frontend cache, CSS, JS, media, Varnish, warm queue, or object-cache behavior changes intended.

## 2.56.144

- Refactor: extracted core `ucwp_*` procedural helper functions from `ultracache.php` into `includes/core/functions.php`.
- Added `includes/core/index.php` for directory safety.
- Kept existing function names, load order, hooks, cache engine behavior, REST/WP-CLI behavior, and frontend optimization behavior unchanged.
- This is the first safe bootstrap-structure cleanup step toward a more modular public-release codebase.

## 2.56.143

- Hotfix: fixed Google Fonts dashboard diagnostics after 2.56.142 by using the UltraCache engine singleton instead of constructing the private engine class.
- Added a defensive filesystem fallback for Google Fonts cache diagnostics so the dashboard can still show built CSS/font counts if the engine summary path is unavailable.
- Added `changelog.txt` as the dedicated release-notes file.
- Reworked `readme.txt` and `README.md` to describe what the plugin does for users, supported cache technologies, a quick start guide, troubleshooting, and all WP-CLI commands instead of carrying long technical release notes.

## 2.56.142
- Fix Google Fonts dashboard status after Rebuild Google Fonts Cache by storing the last scan result, refreshing diagnostics, and showing built/no-remote-URLs/partial-cache states accurately.

## 2.56.141
- Release candidate cleanup: bumped metadata/readme version and removed stray control characters from Varnish/server cache detection regular expressions. No runtime feature behavior changes.

## 2.56.140
- Public release audit fixes: moved conversion support details into the AVIF/WebP Batch Conversion box, hardened media queue REST args, removed REST media format aliasing, added local URL guard to Inspect URL, and added destructive filesystem allowed-root guards.

## 2.56.139
- UI polish: keep Warm Cache action buttons in a single vertical column.
- UI polish: add spacing between media conversion operation counters.

## 2.56.138
- Moved AVIF/WebP batch conversion into its own dashboard box below Media Optimization.
- Added visible buttons for Start/Resume Conversion, Rebuild Media Queue, Verify/Repair Queue, Retry Failed, and Clear Completed Queue Rows.
- Moved media conversion live progress/logs into the Batch Conversion box only; cache warm-up UI is unchanged.
- Added media-only operation copy and counters for attachments checked, image units checked, AVIF generated, WebP generated, already optimized, and failed.
- Added REST endpoints for media queue status, rebuild, process, repair, retry failed, and clear completed.

## 2.56.137
- Media queue UX/state safety: complete queues now report already optimized instead of running unnecessary batches.
- Added optimized-storage missing detection and repair requeue path when AVIF/WebP output folders disappear.
- Limited media queue rebuilds preserve existing completed queue state.
- Hotfix: cleaned the media conversion WP-CLI contract so `--format` controls output only and `--media-format` controls image targets.
- Fixed media conversion dashboard progress so attachment progress cannot exceed the queue total while image units are tracked separately.
- Improved media queue building and pause display copy so queue-building state is explicit.

## 2.56.135
- Added a persistent media conversion queue table for resumable AVIF/WebP processing.
- Media conversion now processes pending queue items instead of re-scanning the full library for every run.
- Added queue status, rebuild, process, retry-failed, and clear-completed support to the WP-CLI media command.
- Admin media diagnostics now show conversion queue pending/done/failed/skipped counts.

## 2.56.134
- Polished cache storage diagnostics so CSS bundle file counts use recognized bundle files instead of double-counting delayed-font CSS files.
- Show capped storage scans as minimum values in the dashboard, including AVIF/WebP counts such as 8,000+.
- Clarified WP-CLI cleanup reporting with recognized CSS bundle before/after counts, old orphan-like files eligible for cleanup, and recent orphan-like files protected by grace.

## 2.56.132
- Fixed a WP-CLI scheduled cleanup fatal by declaring the static warm-suppression flag used while cleanup purges cache.

## 2.56.131
- Fixed **Consolidate Remaining CSS** so the leftover CSS bundle pass follows the independent `leftoverCssBundleEnabled` setting in `safe`, `aggressive`, and `full` CSS bundle modes. The selected main CSS bundle mode no longer silently disables leftover consolidation, and leftover-generated files now use a semantic `bundle-leftover-*` filename prefix.

## 2.56.130
- UI polish pass: move Font Pipeline Diagnostics into Advanced Diagnostics, simplify CSS Bundle Summary, remove duplicate Speed Diagnostics CSS source list, add feature request support link, and compact Media Batch support info.

## 2.56.129
- Security/cache correctness audit pass: secret redaction, hard sensitive-query cache bypass floor, and diagnostics.

## 2.56.128
- Varnish/Reverse Proxy UI clarity build. Clarifies HTTP endpoint vs admin-secret modes, improves status diagnostics, dynamic effective purge method labels, and secret-safe admin status display.

## 2.56.127g
- Makes CLS image-dimension optimization use the faster img-only regex path by default to reduce STORE rewrite cost.
- Adds per-request CLS image dimension resolution caching for repeated image URLs.
- Keeps the precise WP_HTML_Tag_Processor path available behind the `ucwp_cls_dimensions_use_html_tag_processor` filter.

## 2.56.127e
- Adds Frontend Rewrite Stage Breakdown to Speed Diagnostics so slow HTML rewrite stages are visible.
- Fixes CSS duplicate diagnostics so single non-blocking delayed-font links no longer count as duplicates.
- Clarifies that profiler sub-stage timings are diagnostic and may not add up exactly due nested wrappers.

## 2.56.127d
- Deepened the Speed Diagnostics UltraCache overhead probe for template_redirect and buffering setup timings.
- Ignored noscript stylesheet fallbacks in critical-path duplicate/mixed CSS diagnostics to avoid false blocking reports.
- Add Speed Diagnostics UltraCache overhead probe for maybe_start_buffering sub-steps.
- Add duplicate/mixed-status CSS link diagnostics for delayed-font and bundle links.

## 2.56.127b
- Renames the admin Performance Profiler UI to Speed Diagnostics with clearer user-facing button labels.
- Saves the timing breakdown immediately for profiler-triggered STORE requests while keeping normal visitor store bookkeeping deferred.
- Replaces the technical missing STORE profile message with a clearer timing-breakdown diagnostic message.

## 2.56.127
- Polishes object-cache visibility without changing the object-cache engine/drop-in behavior.
- Reports the real fallback backend dynamically in stats instead of assuming APCu.
- Adds fallback message metadata to object-cache stats for clearer Redis → APCu/runtime reporting.
- Makes APCu warnings less alarming when APCu is used only for analytics/shared-memory and not as the active object-cache backend.

## 2.56.122
- Makes CSS bundles proxy-stale-safe by preserving `css-bundles/` during purge and using cleanup grace for old bundles.
- Treats main CSS bundles and delayed-font companion CSS files as a lifecycle pair.
- Adds cached HTML validation for missing css-bundle references and resets cron warm-up queue after cache flush.

## 2.56.121
- Regression fix: restores the dependency-aware ordered delayed-loader path for same-host scripts instead of forcing native `defer` for every local asset.
- Prevents grouped inline-before / inline-after configs from running out of order for integrations such as Complianz, Google Site Kit, WooCommerce and similar scripts.
- Keeps **JS Delay / Defer Exclusions** and hard dependency blockers as the final priority.
- Normalizes delayed script URLs to absolute public URLs while preserving the original source for diagnostics.

## 2.56.116
- Tightens **Defer all JS** into a truly aggressive mode by removing legacy conservative JS Delay / Defer exclusions when the switch is enabled.
- Keeps only the absolute dependency floor out of **Defer all JS**, such as jQuery and core WP globals/dependencies.
- Stops generic inline data/localization from automatically blocking **Defer all JS** so WooCommerce/theme scripts can be deferred when the aggressive switch is explicitly enabled.

## 2.56.115
- Adds a new **Defer all JS** toggle under **Frontend JS & Request Chains** for aggressive native defer on eligible scripts.
- Keeps only an absolute dependency floor out of **Defer all JS**, including jQuery, inline-coupled scripts, and core WP globals that commonly break when moved.
- Renames the shared visible exclusions panel to **JS Delay / Defer Exclusions** and applies it across defer, delay, LCP boundary defer, and related JS optimizations.

## 2.56.114
- Adds a visible **Lazy MailerLite nonce refresh** toggle in **Frontend JS & Request Chains**.
- Prevents MailerLite `ml_create_nonce` admin-ajax calls from running on page load by returning a local temporary success response.
- Refreshes the real MailerLite nonce on first form interaction or immediately before submit, then continues the normal MailerLite submit flow.
## 2.56.112
- Allows **Advanced Runtime Font CSS Rewrite** to run during slider/hero safe mode while still respecting broad **Frontend Safe Mode**.
- Preserves current-request original-to-optimized font CSS mappings so the runtime MutationObserver can be injected even after server-side font links have already been rewritten.
- Adds `data-ucwp-font-css-map-source` diagnostics to show whether the runtime map came from cache, current request, HTML links, or bundle manifest.

## 2.56.110
- Adds **Delay icon font-face blocks**, an opt-in CSS bundle feature that moves matched icon-font `@font-face` blocks into a delayed non-render-blocking font stylesheet.
- Adds visible/editable **Delay These Fonts / Patterns** and **Never Delay These Fonts / Patterns** fields, plus a broad icon-font auto detector.
- Adds delayed font diagnostics to **CSS Bundle Summary** and last CSS bundle warm reporting.

## 2.56.109
- Adds **Full CSS Bundle** as a normal **CSS Bundle Mode** alongside Safe and Aggressive. Full mode consolidates all eligible local stylesheet links into the generated bundle while preserving non-all media rules with `@media` wrappers.
- Replaces the two CSS bundle mode toggles with a single **CSS Bundle Mode** selector: Safe, Aggressive, and Full CSS Bundle.
- Reworked the SR7 LCP runtime helper to use the warmed/static LCP preload URL first, removing interval-based DOM area scans and repeated `getBoundingClientRect()` measurements that could appear as forced reflow in Lighthouse/PageSpeed.
- Keeps the 2.56.106 SR7 module-background preload mapping fix intact, including generated `/revslider/o/` to real media-library source resolution.

## 2.56.105
- Adds debug attributes to LCP preload links so diagnostics can distinguish the plugin preload winner from the browser actual LCP request.
- Tightens the SR7 runtime helper so the scoped winner is re-evaluated repeatedly while SR7 paints module backgrounds and slider layers.
- Prefers scoped visible SR7 module backgrounds over decorative/generated RevSlider image-list assets when marking runtime LCP candidates.
- Keeps CSS bundle, warmup, purge, and defer/delay behavior unchanged.

## 2.56.100
- Refines SR7/Revolution Slider LCP detection so static/shared slide backgrounds are preferred over decorative rotating slide layers.
- Adds static-slide aware SR7 LCP diagnostics via `data-ucwp-sr7-role`, `data-ucwp-lcp-reason`, and `data-ucwp-lcp-score` markers.
- Uses the SR7 module/container visual boundary for static-slide LCP candidates instead of the DOM-last static slide node.
- Keeps CSS bundle, safe/functional third-party delay, and runtime dependency guards unchanged.

## 2.56.98
- Uses `delaySafeThirdPartyJsEnabled` as the clean safe-third-party delay switch; the old generic third-party switch is no longer used.
- Adds delayed inline companion handling so matching WordPress before/extra/after inline script blocks execute with their delayed external script in DOM order.
- Updates the delayed script loader to execute delayed inline script markers as well as delayed external scripts.
- Keeps the 2.56.97 safe/functional pattern engine and LCP Boundary Defer semantics intact while fixing functional-delay runtime dependency ordering.

## 2.56.97
- Renames the third-party delay UI to **Delay safe third-party JS** and adds **Delay functional third-party JS** for visible widgets such as consent/cookie scripts, captcha, maps, chat, booking, embedded forms, and reviews.
- Adds visible/editable safe third-party patterns, functional third-party patterns, and third-party delay exclusions with Populate Defaults support.
- Replaces the targeted gtag-only delay pass with a general pattern-based third-party delay engine and diagnostic delay reasons.
- Corrects **LCP Boundary Defer** so it uses the detected LCP image position as the HTML boundary and delays eligible local scripts printed after that image.

## 2.56.96
- Adds a targeted HTML delay pass for external Google gtag.js loader scripts when Delay Third-party JS is enabled.
- Preserves visible user JS Delay / Defer Exclusions while bypassing generic inline-segment blocking only for queue-safe analytics loaders.
- Marks delayed analytics loader tags with `data-ucwp-delay-reason` for clearer diagnostics.

## 2.56.95
- Changes safe page CSS bundle application from duplicate injection to manifest-based conservative replacement, removing only bundled source stylesheet links while leaving unmatched/excluded/runtime links intact.
- Gives safe CSS bundles real request-reduction behavior without switching to the broader aggressive eligibility rules.

## 2.56.94
- Treats frontpage HTML + CSS warm as verified when the loopback HTTP client times out but post-warm cache inspection confirms cached HTML exists and contains the CSS bundle.
- Keeps the timeout visible in the command message while avoiding a false error exit for verified cache writes.
- Adds a `warmVerifiedAfterTimeout` result flag for diagnostics and REST consumers.

## 2.56.93
- Allows safe CSS bundle injection to run on Slider Safe Mode pages instead of building orphaned bundles that never appear in cached HTML.
- Adds final warm verification to frontpage CSS bundle builds, including bundle bytes, cached HTML bundle refs, and stylesheet link counts.
- Reports a warning/error-style result when a CSS bundle is built but the final cached HTML warm fails or does not contain the bundle reference.

## 2.56.92
- Adds Async Remaining CSS decision diagnostics to STORE/CSS diagnostics so applied, skipped, and unresolved stylesheet decisions show explicit reasons.
- Runs Async CSS after font CSS optimization so excluded local stylesheets can still receive font-display rewrites before async eligibility is evaluated.
- Keeps CSS Bundle Exclusions outside generated CSS bundles while still allowing eligible excluded local stylesheets to pass through self-hosted font CSS optimization.
- Allows Async Remaining CSS and Aggressive Async CSS to run even when slider-safe mode is enabled; CSS Bundle Exclusions no longer suppress those passes.
- Fixes font-display injection so minified @font-face blocks receive a valid semicolon before `font-display: swap`.

## 2.56.89
- Changes CSS diagnostics source actions from copy-only to Append exclusion line inside the visible CSS Bundle Exclusions editor.
- Deduplicates appended CSS exclusion lines against the current textarea draft and marks covered suggestions as Already in exclusions.
- Clarifies that CSS Bundle Exclusions keep matching stylesheets outside generated bundles and loaded normally as original stylesheet links.
- Keeps CSS exclusion actions manual: diagnostics do not automatically save or apply exclusions until the user saves the editor.
- Preserves the 2.56.88 Advanced Settings layout with CSS Bundle Exclusions and JS Delay / Defer Exclusions at the bottom of Advanced Settings & Exclusions.

## 2.56.84
- Polishes JS Delay Scan results into live-count sections: Missing recommended, Already listed recommended, and Review-only detected.
- Recomputes missing/already-listed counts from the current JS Delay / Defer Exclusions textarea so the header and append button cannot show stale missing values after populate/append/manual edits.
- Renames the append action to Append Missing Recommended and only enables it when appendable scan results are missing from the visible textarea.
- Shows Missing, Already listed, Recommended, Detected, and Review-only counters so users can see exactly where missing exclusions are.


## 2.56.80
- Adds a full-width JS Delay / Defer Exclusions panel with Populate Defaults, Scan Latest Profile, Append New Suggestions, and Save controls.
- Adds JS Delay Safety Scan diagnostics to STORE profiles for inline handler/global dependency breaks caused by delayed scripts, with high-confidence suggested visible exclusions and duplicate-safe appending.

## 2.56.77
- Refines Fix sliders / hero sections asset protection so generic words like `slider`, `carousel`, `slideshow`, and `hero` are no longer used for URL/handle protection. This avoids false-positive protection for non-hero assets such as product-filter range sliders while keeping broad generic terms for markup detection only.

## 2.56.76
- Adds Critical Request Chain diagnostics to the Performance Profiler, showing render-blocking CSS/JS candidates, delayed/protected script status, protection reasons, origins, locations, and suggested next actions without changing runtime loading behavior.

## 2.56.75
- Adds visible suggested CSS Bundle Exclusion lines for top CSS bundle sources in the Performance Profiler source list.
- Adds copy-to-clipboard controls for suggested exclusion lines so operators can move heavy sources to the editable CSS Bundle Exclusions field after visual testing. Runtime CSS loading behavior is unchanged.

## 2.56.74
- Adds CSS bundle critical-path diagnostics to the Performance Profiler summary, including bundle bytes, source stylesheet bytes, largest source, top CSS source contributors, and render-blocking stylesheet counts.
- Adds large CSS bundle warnings when the generated bundle crosses diagnostic thresholds. Runtime CSS loading behavior is unchanged.

## 2.56.73
- Replaces site-specific placeholder examples in Advanced Settings textareas with generic plugin-safe examples.
- Updates Manual LCP Hero / Slider selector copy so placeholders do not reference the development/test site. Runtime behavior is unchanged.

## 2.56.72
- Maps selected local LCP preload candidates to existing one-to-one UltraCache AVIF/WebP equivalents when media optimization is enabled, while keeping same-origin image preloads free of `crossorigin`.

## 2.56.71
- Adds a generic LCP candidate scoring engine inside the visible Manual LCP Hero / Slider selector scope, covering img/srcset, SR7 attributes, inline `background-image`, and shorthand `background: url(...)` sources.
- Prefers rendered high-confidence hero/background URLs, including actual AVIF/WebP URLs present in the HTML, instead of relying on the first marked SR7 image only.
- Removes `crossorigin` from same-origin image preloads and adds image MIME `type` hints so CSS background preloads can be reused by the browser without credentials-mode mismatch warnings.

## 2.56.70
- Adds a visible Manual LCP Hero / Slider selector field for high-confidence hero/slider targeting, accepting generic entries like `#main-hero`, `homepage-slider`, or `.hero-slider`.
- Uses a found manual hero/slider selector as the preferred SR7 LCP preload scope so the first marked candidate in that block drives the preload target.
- Keeps SR7 runtime LCP priority, slider-aware Boundary Defer, protected slider script exclusions, and HTTPS-safe local Google Fonts URLs unchanged.

## 2.56.68
- Normalizes UltraCache local Google Fonts URLs inside generated CSS to root-relative `/wp-content/cache/ultracache/google-fonts/...` paths so HTTPS pages do not request blocked `http://` font assets.
- Rewrites existing local Google Fonts CSS cache files when reused, so old cached `http://` font URLs are corrected without deleting the downloaded font binaries.

## 2.56.67
- Keeps SR7/Revolution/Swiper/Slick runtime assets protected while boundary defer remains slider-aware and conservative.
- Updates admin copy to explain that Fix sliders / hero sections is a protection layer, not a replacement for LCP Image Priority.

## 2.56.66
- Adds an admin beforeunload guard while queued settings, save requests, dashboard actions, or long admin processes are still running.
- Keeps queued/running dashboard action toasts persistent until success or error replaces the same notice.
- Improves critical cache setting save UX so cache-impacting toggles stay visibly queued/saving and dashboard actions wait for the save to finish.

## 2.56.65
- Removes stray Google Fonts preconnect/dns-prefetch hints after CSS Aggressive Bundling folds the original Google Fonts stylesheet into the generated external bundle.
- Keeps the 2.56.64 aggressive fallback cleanup intact: no per-original noscript fallback links are restored.

## 2.56.64
- Stops CSS Aggressive Bundling from adding per-original noscript fallback links after replacing matched local stylesheets with the generated bundle.
- Adds Performance Profiler CSS output diagnostics for final HTML size, inline CSS bytes, fallback link/marker counts, and noscript count.
- Adds a visible profiler warning when Inline CSS Bundling creates large cached HTML, without silently overriding the user's inline setting.

## 2.56.63
- Corrects the saved Performance Profiler summary mode so callback profiler results show as `callback` in the last-profile endpoint and UI instead of looking like compact-only profiles.
- Improves the profiler help text to explain when the tool is useful and what question each run type answers.
- Shows up to 12 plugin/theme/core timing groups and improves mobile wrapping for profiler action buttons.

## 2.56.62
- Converts the wp-admin Performance Profiler into a collapsed accordion to keep the dashboard cleaner.
- Improves the profiler helper copy so it explains when the tool is useful instead of describing internal mechanics.
- Adds a Plugin / Theme Time Summary that aggregates callback profiler time by plugin, theme, and WordPress core.
- Adds dedicated spacing for the profiler action buttons so the controls no longer appear stuck together.

## 2.56.61
- Adds a manual Performance Profiler card to wp-admin for Compact STORE, Verbose STORE, and Callback Profiler runs.
- Keeps profiler activation request-scoped and explicit; no persistent frontend profiler setting is introduced.
- Adds summary display plus Download JSON and Clear Last Profile actions.

## 2.56.60
- Makes the default STORE profile compact by recording only key lifecycle/rewrite checkpoints, while `X-UltraCache-Store-Profile-Verbose: 1` or `?ucwp_store_profile_verbose=1` keeps the full diagnostic checkpoint stream.
- Removes automatic `hook_summary` expansion from normal STORE profiles; hook callback summaries are now verbose-only.
- Keeps manual callback profiling separate and explicit via the existing callback profiler trigger, without leaking into normal STORE/HIT requests.

## 2.56.59
- Cleans up the temporary wide init/debug profiler checkpoints from the STORE profiler while keeping the manual callback profiler available only when explicitly requested.
- Limits the diagnostic `X-Ultra-Cache-Source` header to explicit debug requests via `X-UltraCache-Debug: 1`, reducing normal production header noise.
- Keeps the 2.56.58 runtime-config resync after full purge so advanced-cache HIT delivery remains active immediately after purging.

## 2.56.58
- Rebuilds the runtime-config sidecar immediately after full purge so the next anonymous HIT can be served by the advanced-cache drop-in instead of falling back to the later WordPress engine early-hit path.
- Adds an explicit `X-Ultra-Cache-Source` response header for advanced-cache vs WP-engine HIT diagnostics.

## 2.56.57
- Makes deep callback profiling fully manual/opt-in via `X-UltraCache-Callback-Profile: 1` or `?ucwp_callback_profile=1`, and only when STORE profiling is also enabled.
- Avoids registering diagnostic lifecycle/callback wrappers on normal frontend requests, protecting regular STORE/HIT performance.
- Treats profiler query args as diagnostic-only so they do not force query-arg cache bypass in WordPress cacheability checks.

## 2.56.56
- Moves object-cache support checks in frontend settings sanitization to cached/read-only mode; live support checks remain in admin, REST, AJAX, WP-CLI, activation, and settings-save flows.
- Enriches profiler slow-callback checkpoints with callback, plugin/origin, file, hook, and priority fields; init target callback timings now feed the same callback timing summary.

- 2.56.55: Adds guarded callback timing profiler for `wp_enqueue_scripts`, `template_redirect`, loader filters, srcset, and shutdown; repetitive settings checkpoints are verbose-only.
- 2.56.54: Skips live frontend compression loopback probes during normal settings/default sanitization; cached/browser diagnostics remain available.
- 2.56.53: Adds an opt-in early buffer/cacheability micro-profiler around maybe_start_buffering(), should_bypass_cache(), settings loading, and support-probe sanitization to isolate the remaining pre-early-HIT STORE delay.
- 2.56.40: Hardens the 2.56.39 Google Fonts admin-scan workflow by making legacy live-build queues/events cleanup-only, keeping frontend missing-font handling read-only, and preserving the page-cache stampede lock behavior.
- 2.56.39: Adds page-cache stampede protection for cold concurrent requests, keeps Google Fonts rebuilding manual/admin-controlled without frontend live builds, and avoids leaving server-cron-only Google Fonts rebuild events after settings save.
- 2.56.38: Moves Local Google Fonts Optimization to a controlled admin/save/manual-rebuild scan pipeline, stops frontend Google Fonts build/queue behavior, adds Additional URLs for scanning, and preserves the Google Fonts cache during Flush All Cache.
- 2.56.36: Keeps the 2.56.35 canonical Google Fonts fix, but frontend/loopback requests now only reuse existing local font CSS and queue missing fonts for WP-CLI/server cron without synchronous downloads.
- 2.56.34: Coalesced Google Fonts background builds into a single queue/runner and made the runtime self-hosted font CSS map non-blocking on frontend requests.

## 2.56.55

- Added guarded callback timing summaries to STORE profile diagnostics for `wp_enqueue_scripts`, `template_redirect`, style/script loader filters, `wp_calculate_image_srcset`, and `shutdown`.
- Made repetitive settings getter checkpoints opt-in via `ucwp_store_profile_verbose_settings=1`, keeping normal diagnostic JSON smaller.

## 2.56.54
- Skips live frontend compression loopback probes during normal settings/default sanitization; cached/browser diagnostics remain available.

## 2.56.53

- Added opt-in early buffer/cacheability micro-profiling, limited to profiled requests, to isolate the remaining maybe_start_buffering → early_hit_check delay.
- Added checkpoints around should_bypass_cache(), engine settings loading, dashboard settings sanitization, and heavy support probes while preserving frontend behavior.
- Kept the opt-in STORE and deep request lifecycle profiler for rewrite-stage bytes/timings plus checkpoints across plugin load, dependencies, plugins_loaded priorities, setup_theme, after_setup_theme, init, wp_loaded, template_redirect, wp_head, output callback, cache write, and shutdown.
- STORE profiler can be triggered with the `X-UltraCache-Store-Profile: 1` request header or `?ucwp_store_profile=1`; the diagnostic query arg is stripped from the cache key.
- Added `wp ultracache store_profile show --format=json` and `wp ultracache store_profile clear`.
- Added CSS bundle diagnostics for user-controlled Inline CSS Bundling, including more robust inline style byte scanning, external bundle links, fallback counts, and manifest bundle file size.
- Added Populate Defaults buttons for visible safeguard/exclusion lists without forcing a full settings reset.
- Preserved the 2.56.43 Google Fonts architecture: no frontend live builds, no legacy queue recreation, no `google-fonts-pending`, and purge-all still preserves local Google Fonts cache.

## 2.56.40
- Frontend Google Fonts rewriting is read-only when local files are missing: it keeps the original Google Fonts URL and never creates legacy live-build queue data.
- Kept the validated 2.56.39 page-cache stampede lock behavior and the controlled dashboard/WP-CLI rebuild workflow.

## 2.56.39
- Added cold page-cache generation stampede protection so concurrent first hits wait for the first generated HTML cache instead of all rendering/storing independently.
- Settings save no longer depends on WP-Cron for Google Fonts cache rebuild; dashboard button/WP-CLI remain the controlled build paths.

## 2.56.38
- Local Google Fonts Optimization no longer discovers, queues, downloads, or builds Google Fonts assets from live frontend requests. Frontend HTML only rewrites Google Fonts links when the local CSS file already exists; otherwise it keeps the original Google Fonts URL intact.
- Enabling Local Google Fonts Optimization queues a homepage scan through the admin/server-cron path, not through the frontend request path.
- Added Advanced Settings & Exclusions → Additional URLs for Google Fonts scanning with Save Google Fonts URLs and Rebuild Google Fonts Cache controls.
- Rebuild Google Fonts Cache scans the homepage plus configured local URLs, clears/rebuilds only the Google Fonts cache, and downloads the discovered CSS/WOFF assets under wp-content/cache/ultracache/google-fonts/.
- Flush All Cache now preserves wp-content/cache/ultracache/google-fonts/ so local font files are not thrown away during normal page-cache purges.

## 2.56.36

- Kept the 2.56.35 canonical Google Fonts URL/hash behavior.
- Frontend and internal loopback requests now only reuse already-built local Google Fonts CSS; missing font CSS is queued for the real cron/WP-CLI runner instead of being downloaded during the page request.
- Added a short schedule lock so cold concurrency does not create duplicate Google Fonts build events.
- Left original Google Fonts URLs as fallback until the local files exist, so CSS integrity is preserved.


- Fixed the protocol-relative Google Fonts root cause where `//fonts.googleapis.com/...` and `https://fonts.googleapis.com/...` were treated as different hashes.
- Prevented `google-fonts-pending` from blocking page-cache storage; local font generation now stays best-effort and the frontend keeps the valid Google Fonts fallback.
- Kept the 2.56.34 baseline behavior and did not carry over the later experimental frontend CSS branches.

## 2.56.34

- Coalesced Google Fonts background builds into a single queue/runner.
- Made the runtime self-hosted font CSS map non-blocking on frontend requests.

## 2.56.28

- Added single-flight transient locks for Google Fonts CSS and font binary localization to prevent frontend PHP-FPM worker floods during cold cache generation.
- Google Fonts remote requests now use shorter timeouts and fall back to the original Google URLs while another request is already building the local cache.
- Replaced the cron warm-up schedule scan with `wp_next_scheduled()` to avoid walking the full WP-Cron array on dashboard/settings loads.

## 2.56.27

- Added hard single-flight locks for heavy dashboard actions and CSS/frontpage bundle generation.
- Stale dashboard action jobs are now failed automatically instead of blocking future actions.
- Internal loopback requests now carry UltraCache headers so on-entry CSS generation does not recursively amplify PHP workers.
- Pruned dashboard action queue storage to avoid stale running jobs and oversized options.

## 2.56.26

- Linked the Diagnostics and Activity Summary accordions so opening or closing either card toggles both together.
- Added the missing `.text-right` utility rule so status pills and right-aligned diagnostic text render correctly.

## 2.56.25

- Added the Query-string args whitelist Populate action for WooCommerce/taxonomy query keys.

## 2.56.24

- Stabilized page-cache variant creation: query-string HTML cache variants now require an explicit allowlist.
- Added a safety cap for same-path/same-bucket HTML variants to prevent runaway homepage cache files.
- Fixed Diagnostics and Activity Summary visibility so they remain visible when Cache Stats is disabled.

## 2.56.23

- Changed performance profile patches so no profile enables background/scheduled warm-up.
- Aggressive profile now keeps `cronWarmEnabled`, `cronWarmStartAfterCleanup`, and `cronWarmStartAfterManualPurge` disabled.
- Manual warm-up buttons remain unchanged.

## 2.56.21

- Blocked unsafe Varnish HTTP endpoints that point to the public WordPress frontend, especially `domain.com:80`, `domain.com:443`, and unsupported HTTP-mode ports.
- Changed the Varnish HTTP default endpoint to `127.0.0.1:82`; Admin mode remains `127.0.0.1:6082`.
- Added runtime guards so Varnish Test, Flush All, and URL purge refuse unsafe HTTP endpoints even if old options or imports contain them.
- Added diagnostics for old W3 Total Cache / Varnish helper leftovers before enabling Varnish or Object Cache.

## 2.56.19

- Consolidated version reporting to a single `UCWP_VERSION` source and removed the private hotfix bundle duplicate.
- Removed legacy REST namespace registration so only `ultracache/v1` is exposed.
- Removed old runtime secret path fallback loading and kept only the per-site secret file outside the webroot.
- Removed the old WP_CACHE marker normalization path; the current managed block is the only supported marker.

## 2.56.18

- Simplified Varnish HTTP endpoint handling to the current host:port-only model.
- Removed the legacy Varnish endpoint remap that silently changed old Varnish listener ports to the detected frontend endpoint.
- Added an admin-mode fallback endpoint of `127.0.0.1:6082` when admin mode is selected and the endpoints field is empty.

## 2.56.17

- Clarified the Varnish HTTP UI: endpoints are documented as host:port only, and PURGE no longer claims an automatic BAN fallback.
- Aligned the advanced-cache fallback defaults with the dashboard defaults for Woo safe mode and stale-while-revalidate.
- Fixed the runtime `defer_stage_safe` mapping so the computed safe-stage flag is written to config.

## 2.56.15

- Fixed a dashboard crash in Advanced Diagnostics caused by an undefined `objectFallbackActive` variable.
- Canonicalized saved dashboard settings on load so invalid previous combinations like Slider Safe Mode + LCP Boundary Defer are written back as valid settings.
- Restricted `data-ucwp-sr7-lcp` to SR7/RevSlider image candidates instead of allowing normal site images, such as the logo, to receive SR7-specific markers.
- Added generic `data-ucwp-lcp` markers for non-slider LCP image candidates.

## 2.56.09

- Hoisted CSS `@import` rules to the top of generated page CSS bundles so browser import ordering remains valid.
- Preserved a single CSS `@charset` rule at the very top of the generated bundle when source stylesheets include one.
- Rewrote relative `@import` URLs and normal `url(...)` references against each source stylesheet URL before bundling.
- Added an HTML `Content-Type` guard before `warm_url()` writes static HTML cache files or scans HTML for CSS bundling.
- Reduced per-page CSS warmups from `warm -> bundle -> warm` to `bundle -> warm`, avoiding the extra final loopback pass.

## 2.56.07

- Updated the Warm Cache button labels so they explicitly show Homepage, Shared, or Separate CSS Bundle behavior according to the selected CSS Bundling Scope.
- Homepage/shared scope CSS warm actions now build the homepage/shared CSS bundle once, then warm the selected URL set as HTML.
- Per-page scope CSS warm actions build separate CSS bundles for each warmed menu/full-site URL.

## 2.56.06

- Scoped the WordPress admin `#wpcontent` padding-left override to the UltraCache dashboard only.
- Removed the default WordPress left gutter from the UltraCache full-background admin UI while leaving all other wp-admin pages untouched.

## 2.56.05

- Added an **LCP Optimization** master switch under **Advanced Settings**.
- Gated **LCP Image Priority**, manual LCP image URL, and **LCP Boundary Defer** behind the new master switch while keeping backward compatibility for already-enabled installs.
- Added SR7/Revolution Slider hashed asset discovery for generated `/revslider/o/` images, with preference for existing UltraCache AVIF/WebP cache variants.
- LCP Boundary Defer can now use the detected SR7 module boundary as a conservative visual boundary, while existing protections still apply: WordPress core, WooCommerce core, Elementor, sliders, dependencies, and manual exclusions stay protected.

## 2.56.02

- WP-CLI: `wp ultracache purge --all` is now accepted as an explicit full-cache purge alias for `wp ultracache purge`.
- Settings cleanup: deprecated local-build keys no longer leak into CLI/settings output (`cronWarmStartAfterFlush`, `warmAfterScheduledCleanup`, `avifConversionEnabled`).
- No tracking-query default changes were made.

## 2.56.00

- Expanded parser-assisted HTML rewrite refactor across script/link asset cleanup and LCP image handling.
- Added parser-assisted script/link removal for asset cleanup and frontend authoring asset stripping.
- Improved LCP image candidate handling for `srcset`/`source`-style candidates and parser-backed priority attributes.
- Kept legacy regex fallbacks and HTML rewrite safety bailouts active.

## 2.55.98

- Refactored stylesheet/link rewrite paths to prefer `WP_HTML_Tag_Processor` where possible.
- Preserved legacy fallbacks and safe `noscript` stylesheet fallbacks.

## 2.55.97

- Added deeper parser-assisted preload duplicate checks and shared safe head injection helpers.

## 2.55.96

- Removed theme-specific built-in JS protections. Site/theme-specific issues should be solved with manual exclusions.

## 2.55.90

- Removed border styling from diagnostic/status text pills and value rows.
- Renamed CSS Bundle Diagnostics to CSS Bundle Summary and added it to Activity Summary.

## 2.55.88

- Cleaned Media Optimization naming so `mediaOptimizationEnabled` is the master switch and output mode controls AVIF/WebP/Auto policy.

## 2.55.84

- Added Disk Object Cache and Varnish Admin mode warnings.
- Added analytics backend live probe for APCu/Redis.

## 2.55.81

- Changed analytics hits to APCu → Redis → disabled.
- Changed object cache strategy to Redis → APCu → runtime-only, with Disk only as explicit advanced/debug mode.
